% validate parameters for piecewise anchor points approximation 
function val_param = validatePAncAparam(param)
    if nargin == 0 || isempty(param)
        val_param = validatePLARDPparam;
    else
        val_param = validatePLARDPparam(param);
    end
    
end