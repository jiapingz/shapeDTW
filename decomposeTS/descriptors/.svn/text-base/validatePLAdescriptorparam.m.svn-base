function val_param = validatePLAdescriptorparam(param)
    
    if nargin == 0 || isempty(param)
        val_param.segNum = 10;
        val_param.fit = 'regression';
        return;
    end

    val_param = validatePLAparam(param);

end