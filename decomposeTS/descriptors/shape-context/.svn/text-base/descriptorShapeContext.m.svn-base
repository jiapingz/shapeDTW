% compute shape context descriptor for input subsequence
function rep = descriptorShapeContext(subsequence, param)    
    val_param = validateShapeContextparam(param);
    
    rep = ShapeContext(subsequence(:), val_param);
end