
function cIdx = retrieveChildrenIdx(t, pIdx)
% inputs:
%           t - tree
%           pIdx - a parent node index
% output:
%           cIdx - the indices of Children

    parents = t.Parent;
    cIdx = find(parents == pIdx);
    
end